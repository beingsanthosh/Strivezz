#include<bits/stdc++.h>
using namespace std;
int main()
{//ARRAY
	// int n=5;
	// int arr[n];
	// for(int i=0;i<n;i++)
	// {
	// 	cin>>arr[i];
	// }
	// for(int i=0;i<n;i++)
	// {
	// 	cout<<arr[i];
	// }
	//VECTOR
	// int n=5;
	// vector<int>vet(n);
	// for(int i=0;i<n;i++)
	// {
	// 	cin>>vet[i];
	// }
	// for(int i=0;i<n;i++)
	// {
	// 	cout<<vet[i]<<" ";
	// }
	// for(auto i=vet.begin();i!=vet.end();i++)
	// {
	// 	cout<<(*i);
	// }
	// for(auto i=vet.rbegin();i!=vet.rend();i++)
	// {
	// 	cout<<(*i);
	// }
	// reverse(vet.begin(),vet.end());
	// sort(vet.begin(),vet.end());
	// vet.size();
	// vet.front();
	// vet.back();
	// vet.clear();
	// vet.erase();
	// vet.emplace_back();
	//STACK
	// int n=5;
	// stack<int>st;
	// for(int i=0;i<n;i++)
	// {
	// 	int x;
	// 	cin>>x;
	// 	st.push(x);
	// }
	// while(!st.empty())
	// {
	// 	cout<<st.top()<<" ";
	// 	st.pop();
	// }
	// st.size();
	//QUEUE
	// int n=5;
	// queue<int>qu;
	// for(int i=0;i<n;i++)
	// {
	// 	int x;
	// 	cin>>x;
	// 	qu.push(x);
	// }
	// while(!qu.empty())
	// {
	// 	cout<<(qu.front())<<" ";
	// 	qu.pop();
	// }
	// qu.size();
	// qu.front();
	// qu.back();
	// priority_queue<int,vector<int>,greater<int>>pq;
	// int n=5;
	// for(int i=0;i<n;i++)
	// {
	// 	int x;
	// 	cin>>x;
	// 	pq.push(x);
	// }
	// while(!pq.empty())
	// {
	// 	cout<<(pq.top())<<" ";
	// 	pq.pop();
	// }
	//DEQUE
	// deque<int>dq;
	// int n=5;
	// for(int i=0;i,n;i++)
	// {
	// 	int x;
	// 	cin>>x;
	// 	dq.push_back(x);
	// }
	// dq.push_front(1);
	// dq.pop_back();
	// dq.pop_back();
	// while(!dq.empty())
	// {
	// 	cout<<(dq.front())>>" ";
	// 	dq.pop();
	// }
	// map<int,int>mp;
	// for(int i=0;i<5;i++)
	// {
	// 	int x;
	// 	cin>>x;
	// 	mp[x]++;
	// }
	// for(auto i:mp)
	// {
	// 	cout<<(i.first)<<" ";
	// 	cout<<(i.second)<<" ";
	// }
	// mp.find();
	// mp.clear();
	// mp.erase();
	pair<string,int>pr;
	pr.insert({"santhosh",1});
	pr.insert({"vijay",2});
	pr.insert({"mahi",3});
	pr.insert({"san",4});
	for(auto i:pr)
	{
		cout<<(i.first)<<" ";
		cout<<(i.second)<<" ";
	}

}
